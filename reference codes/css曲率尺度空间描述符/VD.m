%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%进行奇异值分解，求的P矩阵
%%%%%%%%
%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
function Pij=VD(Gij) 
[T,D,U]=svd(Gij);
[a,b]=size(D);
c=max(a,b);
for i=1:c
    if(D(i,i)~=0) %%%%%%%%%%判断对角线元素是不是等于0
        D(i,i)=1;
    end
end
Pij=T*D*U;
end